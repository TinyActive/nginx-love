# Copy to config.env and adjust for local/docker testing
WAF_URL=https://waf.autogate.cc
# Local backend without public DNS:
# WAF_URL=https://waf.autogate.cc
# CURL_EXTRA_ARGS='--resolve waf.autogate.cc:443:127.0.0.1'
