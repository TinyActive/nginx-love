# This file is no longer needed as we're using SQLite instead of PostgreSQL
# SQLite database will be stored as a file in apps/api/prisma/nginx_waf.db
# No Docker container is required for SQLite

version: '3.8'

# Note: This file is kept for backward compatibility but services are commented out
# To use SQLite, simply run the application - the database file will be created automatically

# services:
#   # PostgreSQL service removed - using SQLite instead
#   # SQLite database file will be created at: apps/api/prisma/nginx_waf.db

networks:
  nginx-love-network:
    driver: bridge